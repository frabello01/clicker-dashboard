import { ComingSoon } from "@/components/coming-soon";

export default function AccountsPage() {
  return (
    <ComingSoon
      eyebrow="02 — Roster"
      title="Accounts"
      description="The registry of social profiles assigned to each device, with their current status (active / checkpoint / banned / paused), assigned proxy, and notes. Built in phase 4."
    />
  );
}
