import { ComingSoon } from "@/components/coming-soon";

export default function WarmupPage() {
  return (
    <ComingSoon
      eyebrow="04 — Conditioning"
      title="Warmup"
      description="Schedule warmup sessions per account: open the app, scroll the feed for N minutes, like/comment with configurable probabilities. Built next, phase 1."
    />
  );
}
