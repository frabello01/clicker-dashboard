import { ComingSoon } from "@/components/coming-soon";

export default function PostsPage() {
  return (
    <ComingSoon
      eyebrow="05 — Output"
      title="Posts"
      description="Schedule reels and carousels per account, attach media from the library, set captions. Built in phases 2 (reels) and 3 (carousels)."
    />
  );
}
