import { ComingSoon } from "@/components/coming-soon";

export default function JobsPage() {
  return (
    <ComingSoon
      eyebrow="06 — Workshop"
      title="Jobs"
      description="Live view of the work queue: warmup runs, scheduled posts, account creation flows. Each row drills into per-step logs from the Nomix run. Built alongside warmup."
    />
  );
}
